spring:
  datasource:
    url: jdbc:h2:mem:tsinjo-preprod;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password:
  flyway:
    locations: classpath:db/migration,classpath:db/migration/preprod
  profiles:
    active: preprod